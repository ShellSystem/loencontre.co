
language: php, js

php:
  - 5.6

before_script:
  - cp .env.travis .env
  - mysql -e 'goodfirm_entregas;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit